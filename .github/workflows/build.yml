name: Build Pipeline
on:
  push:
    branches:
      - '*'
    tags:
      - 'v*'
  pull_request:
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Setup Go
        uses: actions/setup-go@v1
        with:
          go-version: 1.14.x

      - name: Checkout
        uses: actions/checkout@v1

      - name: Test
        run: make test

  build:
    name: Build
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: '13.x'

      - name: Install NPM Dependencies
        run: make npm

      - name: Build Webpack
        run: make webpack

      - name: Build
        run: make build

# release only on tag
  release:
    name: Release
    needs: [build]
    runs-on: ubuntu-latest
    if: contains(github.ref, 'v*')
    steps:
      - name: "I don't know how to release yet"
        run: echo lol



